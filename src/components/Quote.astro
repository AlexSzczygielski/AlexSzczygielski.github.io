---
const { author, note } = Astro.props;
---
<figure class="quote">
  <blockquote><slot /></blockquote>
  <figcaption>
    <span class="quote-author">— {author}</span>
    {note && <span class="quote-note">{note}</span>}
  </figcaption>
</figure>
